using UnityEngine;

public class NumblockTextureController : MonoBehaviour
{
    void Start()
    {
        float width = Screen.width * 0.95f;
        float height = width / 4;

        //guiTexture.pixelInset = new Rect(
        //    (Screen.width / 2) - (width / 2),
        //    (Screen.height / 1) - height,
        //    width,
        //    height);
    }
}
