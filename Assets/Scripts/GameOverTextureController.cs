using UnityEngine;

public class GameOverTextureController : MonoBehaviour
{
    void Start()
    {
        float width = Screen.width * 0.66f;
        float height = width / 4;

        //guiTexture.pixelInset = new Rect(
        //    (Screen.width / 2) - (width / 2),
        //    (Screen.height / 2) + (height * 0.1f),
        //    width,
        //    height);
    }
}
